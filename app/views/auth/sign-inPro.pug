extends ../shared/_layout

block app-title
    title EasyPeasyShop:Sign in

block content
    form(action='/auth/sign-inPro', method='post')
        div
            label Username:
                input(type='text', name='username')
            label Password:
                input(type='password', name='password')
            button(class= 'btn btn-success')
                | Sign in
