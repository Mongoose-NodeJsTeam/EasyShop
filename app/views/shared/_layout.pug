doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        block app-title

        link(rel="stylesheet", href="/libs/bootstrap/dist/css/bootstrap.css")
        link(rel="stylesheet", href="/public/css/main.css")
        block scripts
        script(src="/libs/jquery/dist/jquery.js")
        script(src="/libs/bootstrap/dist/js/bootstrap.js")
    body
        if user
            if !user.isProUser
                include ../partials/navigation/_navigation-logedIn-User
            else
                include ../partials/navigation/_navigation-logedIn-Shop
        else
            include  ../partials/navigation/_navigation

        .container
            block content

        block additionalBottomScripts